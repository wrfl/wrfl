.page-header
  %h1
    - if @user == current_user
      Your Profile
    - else
      Edit User

.row
  .col-sm-6
    = render 'form'

- if can? :destroy, @user
  %hr
  .clearfix
    .pull-right
      = link_to 'Disable User', @user, method: :delete, class: 'btn btn-danger'
